linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gosec
    - bodyclose
    - noctx

issues:
  exclude-rules:
    # Relax security linting in test files.
    - path: _test\.go
      linters:
        - gosec
